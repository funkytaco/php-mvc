; Dev-mode opcache settings, mounted by <app>-compose.dev.yml.
; Revalidate files on every request so host edits are picked up immediately.
opcache.enable=1
opcache.validate_timestamps=1
opcache.revalidate_freq=0
